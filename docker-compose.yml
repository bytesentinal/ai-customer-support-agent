services:
  support-agent:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./docs:/app/docs           # client documents (live-mounted)
      - ./chroma_db:/app/chroma_db # vector DB persists between restarts
    env_file:
      - .env
    restart: unless-stopped